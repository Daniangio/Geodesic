# Geodesic (Frontend)

This is a simple React lobby client for guest sessions and rooms.

## Local development

- `npm install`
- `npm start`

The UI expects the lobby WebSocket at `/api/v1/lobby/ws`. Set
`REACT_APP_WS_BASE` if the backend runs on a different host.
